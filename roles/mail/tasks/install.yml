---
- name: 各種パッケージのインストール
  ansible.builtin.apt:
    name:
      - dovecot-pop3d
      - dovecot-imapd
      - opendkim
      - opendkim-tools
      - opendmarc
      - postfix
      - python3-certbot-dns-cloudflare
    force_apt_get: yes
    update_cache: yes
